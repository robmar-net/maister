{
  "//": "#57 PROTOTYPE registration — a PostToolUse normalizer on Copilot's Edit (apply-patch) file tool. NOT wired into build.sh; overlaying it into plugins/maister-copilot/hooks/ is a separate, governed step gated on the deployment questions in README.md.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/normalize-orchestrator-state.sh", "timeout": 10 }
        ]
      }
    ]
  }
}
